# Shared KB Cross-Skill Review

This iteration is a prompt-and-assertion review pass for the shared knowledge-base cooperation flow.

It contains one review packet per eval case. Each packet includes:

- the user prompt
- the expected cooperation chain
- the assertions to validate later during real runs

This iteration is meant for human review before live with-skill / baseline executions.
